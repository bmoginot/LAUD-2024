initial tests indicate that more sensitive runs cause more reads to map
next i need to do this in python and grab the percent of reads that map out

got percent of reads that map out on full read sets
we want fewer reads to align (idea is the first reads to be dropped will be the ones that are from the strain we don't want)
very slightly fewer reads align with --fast and --very-fast, so we're going to run the final script with --very-fast
TO NOTE: some of the reads had a nearly 0% alignment rate with each parameter, even with the full readsets

i just remebered i have a piece of my script that filters out bladder reads, so i can probably let sleeping dogs lie as it were

wrote a script to subset participant reads from an input list
this will make testing with multiple pats easier and i didn't want to have to edit the script every time i needed a new pat
next, i need to look at lexi's thesis and find some other problematic heatmaps other than 17

pats of interest are 3 (maybe, just rectal), 17, and 20
there might be others but this is fine for testing

cleaned up main script; it now runs bowtie2 on multiple participants at once
a lot of reads have a really low alignment rate not sure what that's about but the info is in the log
next step is running SPAdes (FOR REAL THIS TIME)
